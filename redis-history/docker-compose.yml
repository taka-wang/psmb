redis:
    image: redis
    ports:
        - "6379"

redis-history:
    build: ../.
    dockerfile: redis-history/Dockerfile
    links:
        - redis
    volumes: # mount for test
        - /var/tmp:/var/tmp
        - $PWD/shared:/shared
